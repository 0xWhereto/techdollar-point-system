{
  "name": "techdollar-point-system",
  "version": "0.1.0",
  "description": "On-chain points engine for the TechDollar (USDte) protocol — tracks USDte hold (1×), sUSDte stake (2×), Curve LP (3×), Morpho supply (3×) and produces a time-weighted leaderboard. Designed to be integrated into an Express + Sequelize host app.",
  "license": "UNLICENSED",
  "private": true,
  "main": "src/points/index.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "jest --no-coverage",
    "test:watch": "jest --watch --no-coverage",
    "lint": "eslint src/",
    "smoke": "node scripts/smoke.js",
    "migrate": "node scripts/migrate-points-mint-event.js"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "sequelize": "^6.35.2",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
